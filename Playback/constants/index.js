import images from './images';
import icons from './icons';
import { COLORS, FONTS, SIZES, SHADOWS, horizontalScale, verticalScale, moderateScale } from './theme';

export { images, icons, COLORS, FONTS, SIZES, SHADOWS, horizontalScale, verticalScale, moderateScale };